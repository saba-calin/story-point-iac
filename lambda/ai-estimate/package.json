{
  "name": "ai-estimate",
  "version": "0.1.0",
  "description": "Lambda function that uses ChatGpt to estimate a story based on its name and description",
  "main": "index.ts",
  "scripts": {
    "build": "rm -rf dist && esbuild ./ --entry-names=[dir]/[name]/index --external:pg-native --bundle --minify --platform=node --target=node22.0.0 --outdir=dist"
  }
}
